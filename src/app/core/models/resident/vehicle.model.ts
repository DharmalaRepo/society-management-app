export interface Vehicle {
  vehicleId?: string;
  residentId: string;
  flatNumber: string;
  vehicleNumber: string;
  vehicleType?: string;
  brand?: string;
  color?: string;
  parkingSlot?: string;
}
